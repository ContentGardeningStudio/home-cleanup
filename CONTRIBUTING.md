# Contributing to Home Cleanup

Thanks for your interest in contributing!

## 🧩 How to Contribute
1. **Fork** this repository.
2. **Create a branch** for your change:
   ```bash
   git checkout -b feature/your-feature
   ```
3. **Commit** your changes with clear messages.
4. **Push** your branch:
   ```bash
   git push origin feature/your-feature
   ```
5. **Open a Pull Request** — describe your change and why it’s useful.

## 🧪 Development Setup
- Python 3.9+
- Install dependencies:
  ```bash
  pip install -e .
  ```

## ✅ Code Style
Follow [PEP8](https://peps.python.org/pep-0008/) and include type hints where useful.

## 🧠 Contribution Ideas
- New filters or criteria (e.g., file extension filters)
- Better reporting formats
- Integration with cloud storage cleanup
